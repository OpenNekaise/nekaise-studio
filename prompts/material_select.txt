Choose this round's final material package as the primary teacher. Material Authors only produce candidates; you retain all teaching authority.

The manifest contains immutable job/result hashes, candidate IDs, summaries, exact-source provenance and descriptive checks. Read complete candidates with the read-only archive material_candidates operation (round_id plus offset/limit, optional candidate_id); all candidates and complete provider artifacts remain accessible. Choose your own inspection scope, including group inspection or a justified sampling policy. Record that scope honestly. A manifest preview or a source reference alone does not establish correctness.

Return the exact manifest_hash supplied. accepted_ids selects exact candidate IDs; accepted_jobs selects every exact candidate in those plan job IDs. Use edits for explicit replacement material (candidate_id identifies its original); edited items are selected automatically and must not also be listed individually. Every unselected candidate is omitted. Lists can all be empty. seed_exclusions may omit original seed lessons; it cannot name synthetic candidate IDs. State the reason for your complete package and omissions.

Choose final token_mix and train_epochs for the actual package; your earlier curriculum was a plan. No fixed original/auxiliary ratio, minimum dose, required author count or learning gate applies. Keep source and seed references accurate. An authored example may have no source citation; generated text must not be labelled raw corpus. Native student chat serialization happens at freeze, not at the author API.

These candidates have no student attempts. Do not interpret missing attempts as empty answers or diagnosed failures. Mechanical checks and duplicates are evidence for your decision, not a second semantic teaching gate. You can accept useful material without rewriting it. Prefer meaningful variation over copying; reconsider estimated work using the available actual accounting.

Under config_hints.expansion_policy=required_v1, positive training must include expanded material you accept or edit, with a positive teacher stream share so it reaches the dataset. You may reject every candidate: explicitly choose train_epochs=0 and explain the next expansion correction. Do not accept incorrect material merely to enable training. The operator requires expansion every training round and wants larger useful training exposure per primary-teacher token. Choose useful coverage from generated batches and deliberate passes.

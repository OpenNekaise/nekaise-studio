Plan this CoAPT Mid-training round yourself after inspecting the student history and relevant corpus. Return a Curriculum object.
Apply the operator's generated-general-material contract in the CoAPT handbook. Under general_material_policy=required_v1, positive training needs original general Teacher lessons and general Material Author jobs, with consumed native general-chat targets. Declare material_scope on each lesson and expansion job (general_chat/general_prose/domain); training readings may declare their scope too. Job scope is inherited by candidates. Historical unknown scope stays unspecified. General lessons do not need domain framing. No downloads or streaming of Nemotron-CC, DCLM, FineWeb or Dolma: these inspire breadth only, not actual provenance.
For the first intervention, target 38% general chat + 12% general prose + 50% domain prepared causal targets, one pass; an initial source mix is teacher=.70/corpus=.05/replay=.25 with 20% total fresh domain, 25% domain replay and 5% domain corpus. Size generated general targets to about 5/7 of the new teacher stream. Record planned scope shares in work_plan.material_scope_mix and actual allocation/reasoning in material_strategy. These are starting choices: adapt from evidence while retaining the required general portion. Keep short ordinary requests and varied responses represented; do not merely rename domain/scaffold examples as general. Use native chat_response for general chat revisions and Author outputs, with nonempty answers. The host verifies nonzero requested general exposure/provenance, not semantic content or ratio tolerances. Inspect previous_learning_work.material_portfolio to distinguish planned shares, frozen targets and verified completed exposure.
lessons: any number of tasks, in your chosen order, each with a unique id. Within the unified Mid-training recipe, kind=cpt labels teaching prose and kind=sft labels QA supervision. These are material labels, not separate training modes; you can choose any composition, all of either, or none for a reading/review/diagnostic round. sources lists the document spans you choose (start/length in original file characters; length=0 means the rest). An empty sources list means a teacher-authored exercise. concept labels the subject, prompt is the task/question label, and student_prompt is your exact student content. student_format=campaign uses config_hints.student_format: raw_text continues it literally; chat_template wraps it as one native no-thinking user message. Explicit raw_text or chat_template overrides support deliberate probes. Include or omit source context as you judge useful; the engine never supplies hidden teaching content. Explain the teaching reason.
readings: raw corpus spans to train on; these are explicit selections, not automatically all lesson sources.
replay: explicitly chosen historical lesson references {round_id, lesson_id}, from any campaign or age. The immutable teacher content and selected tokenization mode are replayed; current tokenizer special tokens may differ across Base/SFT lineages. Inspect frozen IDs. You may instead create a new lesson to revise older teaching.
comparison_round_id: empty normally. To investigate checkpoint changes, you may select one completed historical round with a different checkpoint stored in this workspace's runs. The worker verifies its immutable training artifact and runs the same teacher-authored lesson content on the current and reference checkpoints using each saved interface (old artifacts default to raw_text), sequentially within one shared stage timeout. Nonempty lessons are required. Each lesson retains its current student answer and a separate comparison object with the reference round, checkpoint, artifact and generation evidence; both are available to revision and reflection. This neither replaces the current checkpoint nor inserts reference answers into training or assessment. You decide prompts, material, passes and interpretation; train_epochs=0 makes the comparison weight-preserving. comparison_checkpoint defaults to output. Set it to input to compare the selected completed training round's recorded input: its model_before must match the immutable training artifact's parent. A local checkpoint must belong to this workspace's runs, match a completed parent round's immutable training artifact, and pass manifest and file-hash verification. A pinned local Hub snapshot instead requires content-addressed cache-blob verification. The worker freezes file hashes at selection and rechecks them before generation. Arbitrary paths and local inputs without completed parent provenance are unsupported. Snapshot checks establish recorded path and current byte integrity, not a retroactive hash of the original training input. This is evidence only; no restoration or score gate is implied.
token_mix: teacher/corpus/replay shares summing to 1; zero shares are allowed. For a diagnostic round with train_epochs=0, all three shares may instead be 0, leaving no prepared training target tokens. A sum of 1 with train_epochs=0 preserves prepared token samples without training on them. If a stream is absent, available shares are renormalized and reported. Choose train_epochs>=0. train_epochs=0 explicitly keeps the weights unchanged, even if you prepare lessons/readings for future use. With positive train_epochs, config train_steps=0 AUTOMATICALLY computes updates from the prepared tokens and your chosen passes; it does not disable training. A positive operator train_steps overrides that automatic update count only when you enable training. Choose repetition deliberately and explain its teaching purpose. The worker executes these fields; express your training decision here rather than only in notes.
evaluation_instructions: decide the purpose, breadth, question types, difficulty and extent of the online diagnostic after this round's training. The evaluation author can adapt after inspecting this round. notes: record why this plan fits the student, including the recipe's CPT/SFT composition when relevant.
All lists may be empty for a diagnostic-only round. The engine must not label that as weight training. Read the previous teaching strategy and choose freely; do not use a fixed material alternation or fixed question count.
scoring_round_ids: optionally select completed historical rounds that actually trained. The worker scores their exact frozen token samples on each round's verified before/after checkpoints, recording FP32 and (on CUDA) BF16-autocast target/EOS probabilities and token loss. Exact untruncated recorded prompt prefixes allow separate prompt and continuation loss; other samples remain unassigned. This runs in draft before current prompts, sharing its model-execution timeout, and is saved as historical_scoring in the draft artifact and reflection input. You can read that artifact for earlier inspection. It never retokenizes training text, generates replacement answers, updates weights, changes the current checkpoint, or supplies a learning gate. Empty disables scoring. Select train_epochs=0 for an entirely weight-preserving diagnostic round; scoring itself never trains. All historical rounds remain selectable without a recency cutoff, subject to immutable provenance and execution budgets.

For cross-checkpoint comparisons, a per-lesson student_format override applies to both sides. Otherwise interfaces can differ: interpret results as checkpoint-and-interface evidence, not a weights-only ablation. Read campaign_context_artifact when supplied for the authorized experiment and preserved lineage references.

task.previous_learning_work reports the latest completed round's actual token exposure, optimizer updates, inner training time, stage walltime and current checkpoint bytes. Use it when choosing material and train_epochs: round count is not training dose, and a small one-pass dataset can pay a large fixed authoring/loading/saving/review cost for one short update. Explain the chosen dose relative to the previous observation in notes. More material or additional passes can amortize that cost, but neither is required or automatically beneficial. Select a bounded learnability/transfer contrast when uncertainty warrants it, using compare_before evaluations or existing historical scoring. Distinguish trained-example recall, fresh transfer, retention and format; decide when prerequisite practice should reconnect to the campaign's subject. You retain all curriculum, repetition, assessment and continuation choices; no fixed minimum tokens, passes, mix or score applies.

Plan a coherent work package, rather than treating lessons_per_round as a ceiling. config_hints.workload_guidance records the operator's preference for useful training work. Same-checkpoint student prompts now run in bounded batches; execution batch size does not limit how many lessons you can choose. When fixed authoring/loading/saving costs dominate tiny updates, consider a materially larger set of relevant examples, explanations, selected readings and review, with useful variation and coverage. Repeating a handful of examples only to make the GPU busy is not new coverage. A small or diagnostic round remains your choice when its information is worth the cost; explain that purpose.

work_plan records your estimated_targets_per_pass (causal prediction targets including prompt/EOS, before train_epochs), material_strategy and dose_rationale. This is a teacher estimate, not a measurement or a required minimum. Use null only when an estimate is unavailable. The frozen preparation and measured update_work will be returned for comparison at reflection and next selection. Missing that estimate never blocks training.

The current mixture uses the first nonempty positive-share stream in teacher/corpus/replay order as its anchor. All available anchor targets are prepared once per pass; other streams fill the ratios relative to that amount. Therefore adding a large reading list while keeping a tiny teacher anchor and small corpus share can leave most selected reading targets unused. preparation.streams exposes available, prepared, unused and repeated target occurrences; these are not deduplicated content counts. Choose the material and shares together to express your intended coverage. Each dataset pass flushes its final partial update: a 500-target dataset trained twice is two 500-target updates, not two full tokens_per_update batches. Larger useful material and extra repetition are different interventions; decide deliberately.


Material Authors can generate additional teaching material under your plan. task.material_authors lists the frozen available authors, credential availability, request/output allowances and concurrency. You remain the only teaching authority. Under config_hints.expansion_policy=required_v1, EVERY positive-training round must include expansion_jobs and ultimately consume expanded material you selected or edited. Empty jobs are allowed only for train_epochs=0 diagnostics. This explicit operator requirement supersedes older notes/reports saying delegation is optional. If no author is available, preserve weights and identify the operational blocker; do not silently use seed-only training. Choose jobs with a unique id, author_id, corrected seed_ids from this curriculum, optional reading_indices into your readings list, explicit instructions, expected_items and max_output_tokens. Jobs execute after your seed revisions. Different jobs can run concurrently; split by useful coverage, not a fixed author count. Each job receives only its declared seeds/readings and your plan notes. Its output is synthetic candidate material without a student attempt. Do not require a student rollout for every variant unless you choose those tasks separately.

All requested jobs must fit the provided execution allowance. max_output_tokens reserves API output including provider reasoning, which is not automatically training text. Decide output scope accordingly; authors may return fewer candidates, which is reported rather than automatically filled. Expected material counts are not learning outcomes. Under material_review_policy=teacher_review_v1, a later material_select request lets you inspect candidates, accept exact IDs/batches, edit content, omit seeds and choose final shares/passes. Under trusted_author_v1, your expansion_jobs preauthorize all structurally valid generated candidates: there is no later content review, pruning or Teacher selection call. Your curriculum token_mix and train_epochs are final for this package; plan the dose using measured author yield. Trust the generated teaching content and assess the student's learning afterward. You retain the next curriculum, source, author, task and dose choices; zero-pass diagnostics remain available. Jobs requested with zero passes still run and incur author usage. Full candidates and sources remain accessible for teaching use. Reduce repetitive manual authoring by delegating appropriate variation while keeping useful teacher-corrected seed examples.

Use previous_learning_work.author_yield to size expansion from observed output tokens per candidate and prepared targets per selected candidate, alongside material_author_work's all-attempt costs and missing usage. Consult author_yield.by_job for comparable job costs and successful-call reservations; the author-wide average does not bound a different job. Each observed output cost covers the entire serialized batch, including generated student prompts, responses, metadata and reasoning. Shorter student answers alone do not establish a proportional reduction in provider output. Exact job/result artifacts and all older history remain available when needed. These are separate tokenizers and denominators. Prefer a larger useful work package when small packages repeatedly spend much more primary Teacher context than they train. Consider several bounded jobs, including different available authors, with distinct useful variation dimensions and corrected seeds; source relevance and final acceptance remain your decisions. Size expected_items against each author's max_response_output_tokens with room for JSON, reasoning and variation. Spread work across jobs instead of overfilling one response. Leave retry headroom within the existing round allowance. Estimate target coverage in work_plan, then compare it with the actual frozen package next round. Repeated passes and exact content copies do not create new coverage. You may choose a smaller diagnostic when its information justifies that choice; no job count, target dose, author split or efficiency threshold is imposed.

For transport=claude_code, each job's actual response cap is floor(that job's max_output_tokens / 2). The catalog's max_response_output_tokens is the ceiling at the author's maximum reservation, not an allocation every job receives. For example, a 9,000-token job reservation permits 4,500 response tokens; a 32,768-token reservation permits 16,384. The other half covers a possible in-flight continuation during cancellation; usable continuation is not available. Size the entire serialized JSON response, including prompts, answers, field names, rationale and provenance, plus any reasoning. A word/token bound on answers alone does not bound complete rows. Reducing candidate count and reducing the reservation are separate choices: keep sufficient response margin using observed complete-call usage and the intended row size, and budget all job reservations plus retry headroom within the round allowance. You choose the counts, content and reservations; these are execution limits, not a curriculum or minimum-dose rule.

The operator wants more useful measured training exposure and higher Tokens trained / Teacher tokens. task.previous_learning_work.teacher_efficiency reports actual optimizer targets divided by primary-teacher input + output, including cache once and retries. Inspect status/final and missing/pending counts; reported_ratio with incomplete usage is provisional. Author usage is separate in material_author_work. Use a compact set of informative corrected seeds, delegate substantial varied material in bounded jobs, then review/select/edit it. Spend primary-teacher tokens on diagnosis, design and review rather than manually writing every repetitive variant. Explain in work_plan how coverage, package size, mix and useful passes improve actual training tokens per teacher token. Prepared targets, author tokens and consumed exposure differ. Extra passes raise the ratio without new coverage; do not pad text, repeat solely for this metric or remove necessary assessment. Efficiency is feedback, never a learning-score gate.

Teaching experiments: use experiment to record an explicit hypothesis and teaching strategy before this round's student observations and training. Record title, hypothesis, intervention, budget_basis, observation_plan and reconsider_if; related_round_ids may refer to any earlier recorded teaching rounds. State which resource axis you intend to compare, or acknowledge that budgets/starting points differ. Define strategy {name, approach, parent_version} with strategy_version empty, OR reuse a full recorded strategy_version with strategy null. New strategy definitions get an immutable content-hash version; unchanged content reuses it. parent_version is empty for an original strategy or a previously recorded version being revised. task.latest_experiment provides a quick reference; archive experiments (limit/before), experiment (round_id), and strategy (version) expose all prior cards and evidence. Record meaningful changes and continued investigations, including uncertain or unsuccessful approaches; null is valid when you are not proposing an explicit experiment. These fields document your reasoning and impose no teaching recipe, dose, fixed panel or learning-score threshold. The observation plan does not freeze evaluation questions: those are authored later and frozen before answers. A sequential round or comparison with a different starting checkpoint is not a matched-start A/B trial. Future reflection records a separate judgment and cannot rewrite this plan.

Inspect the actual attempts in task.lessons and retrieve any additional teaching history or source material that helps. You decide the correction, explanation style, depth and whether each lesson merits CoAPT Mid-training.
text is the corrected explanation/answer for inspection. training_text is the EXACT sequence you want the student to learn; the worker does not append a question or reformat it in full_text/prompt_prefix modes. Within the shared Mid-training recipe, cpt material is usually self-contained prose; sft material includes the task, necessary context and correct answer in your chosen plain-text format. These raw modes use full-sequence supervision in the same trainer. Put explanations the student should learn INTO training_text; errors/evidence/reason fields are retained as observations, not automatically used as training targets.
Preserve useful correct ideas, address mistakes and omissions, and use your expertise to teach the concept. You may synthesize across sources and add your own examples and knowledge. Evidence is optional documentation, not an admission gate. Never fabricate source quotations or student observations.
Return one row per input ID. use_for_training is your decision; reason explains it. A lesson you omit may have empty text. An empty student attempt is valid diagnostic information. No second reviewer will override your teaching decision.
Choose training_tokenization=full_text for ordinary whole-text tokenization. Optional prompt_prefix separately tokenizes the exact student_prompt and the remaining training_text suffix, preserving the generation prefix even when BPE would merge across that boundary. For this option, training_text must start with the exact student_prompt, including whitespace. It inserts no text or template, and both options retain full-sequence loss. This option changes tokenization, not teaching content; choose it only when useful for your intended continuation.

For native single-turn chat supervision choose training_tokenization=chat_response, training_text="", and training_response equal to your exact assistant continuation only. The worker supplies the same native no-thinking generation prefix and native assistant terminator; do not duplicate user text, role markers or the empty thinking prefill. All tokens still carry loss. A raw recipe remains available even in chat campaigns. Fill training_response="" for other modes. Inspect actual prefix evidence; serialized alignment does not guarantee learning.

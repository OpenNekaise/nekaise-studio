# Independent gym evaluation / rollout environment.
# Keep separate from requirements.txt: vLLM 0.25.1 requires a different validated
# torch/transformers pair than the Unsloth training stack.
vllm==0.25.1
torch==2.11.0
transformers==5.6.0
ninja==1.13.0
